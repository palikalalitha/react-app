import styled from '@emotion/styled'
import tw from 'tailwind.macro'
const TodoFooterContainer = styled.div `${tw`flex m-2 justify-between `}width:500px`;
const CheckBoxElement = styled.div `${tw`w-10 h-4 bg-red-200`}`;
export {
    TodoFooterContainer,
    CheckBoxElement
}
